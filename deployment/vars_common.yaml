static_resource_group: t0-dev-deploy-static
gpu_resource_group: t0-dev-deploy-chatty
location: uksouth
vm_name: chatty-vm
vm_size: Standard_ND96amsr_A100_v4 # Large GPU VM size
# vm_size: Standard_NC48ads_A100_v4 # Testing - Medium GPU VM size
# vm_size: Standard_DS1_v2 # Basic CPU VM size for testing
# vm_size: Standard_NV12s_v3 # Possibly the smallest GPU-enabled VM size

vnet_name: chatty-vnet
subnet_name: chatty-subnet
public_ip_name: chatty_ip
nsg_name: chatty_nsg
nic_name: chatty_nic
ansible_user_id: ansible
ssh_key_file: "~/ssh_keys/id_ed25519_t0_ansible"
t0_user: t0
data_file_path: data/nhs-conditions/v4/qwen_summarised_conditions.jsonl
