## t0/web

Web frontend for t0. Svelte/TypeScript.

### Setup

0. `brew install pnpm`

If you prefer you can install `npm` and replace `pnpm` with `npm run` in all the commands below

1. `cd` into this directory
2. `pnpm serve-rag`
3. `pnpm dev`
4. `open http://localhost:5173`
